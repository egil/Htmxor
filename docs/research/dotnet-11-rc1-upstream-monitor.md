# .NET 11 RC1 upstream-monitor provenance

The .NET 11 monitor baseline is the ASP.NET Core release tag
`v11.0.0-rc.1.26425.128` at commit
`c3325eeb6b47bc6383c127d4f4827dc9642a2b6e`. Its source inventory uses the
same reviewed Htmxor dependency watches as .NET 10, evaluated against the
installed .NET 11 RC1 reference metadata and `NET11_0` preprocessor context.

This is research provenance for source-drift monitoring. It does not approve
runtime compatibility, package support, browser behavior, or a .NET 11 release.
Those claims remain owned by the later compatibility slices in #207.
